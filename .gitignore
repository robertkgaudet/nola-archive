node_modules/
.env
*.local
# confidential shield blocklist must never enter the repo
blocklist*.txt
*blocklist*
vendor*.csv

# run logs stay local — they hold not_found reasons not persisted to the DB
*.log

# stage 3a intermediate artifacts
.stage3-cache/
